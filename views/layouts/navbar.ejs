<nav class="navbar navbar-expand-lg sticky-top">
    <div class="container">
        <a class="navbar-brand fs-3 fw-bold" href="/">
            <i class="bi bi-flower1"></i> RHOPALOCERAZ
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <% if (!user || user.role === 'customer') { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/catalog">Catalogue</a>
                    </li>
                <% } else if (user && user.role === 'admin') { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/dashboard">Dashboard Admin</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/orders">Pesanan</a>
                    </li>
                <% } %>
            </ul>
            
            <ul class="navbar-nav ms-auto align-items-center">
                <% if (!user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/login">Login</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="btn btn-primary" href="/auth/register">Sign Up</a>
                    </li>
                <% } else { %>
                    <% if (user.role === 'customer') { %>
                        <li class="nav-item me-3">
                            <a class="nav-link position-relative" href="/cart">
                                <i class="bi bi-cart3 fs-5"></i>
                                <% if (cartCount > 0) { %>
                                    <span class="position-absolute top-10 start-100 translate-middle badge rounded-pill cart-badge">
                                        <%= cartCount %>
                                    </span>
                                <% } %>
                            </a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                <i class="bi bi-person-circle fs-5"></i> <%= user.nama %>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0">
                                <li><a class="dropdown-item" href="/profile">Profil Saya</a></li>
                                <li><a class="dropdown-item" href="/orders/history">Riwayat Pesanan</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" href="/auth/logout">Logout</a></li>
                            </ul>
                        </li>
                    <% } else if (user.role === 'admin') { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                <i class="bi bi-shield-lock fs-5"></i> Admin
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0">
                                <!-- Link Katalog dipindahkan ke sini agar hanya muncul untuk Admin -->
                                <li><a class="dropdown-item" href="/admin/products">Katalog Produk</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" href="/auth/logout">Logout</a></li>
                            </ul>
                        </li>
                    <% } %>
                <% } %>
            </ul>
        </div>
    </div>
</nav>